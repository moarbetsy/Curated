---
description: Agent protocol — step-ledger discipline and failure loop
alwaysApply: true
---

# Agent protocol
Follow docs/AGENT_PROTOCOL.md.

Use docs/STEP_TEMPLATE.md; append to docs/STEPLOG.md after each step.

If a command fails: STOP. Follow the Failure Loop in docs/AGENT_PROTOCOL.md.
Log real breakage to docs/REPORT.md only; never put secrets there.
