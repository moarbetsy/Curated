---
description: Log real breakage and use /apply-report to resolve incidents.
alwaysApply: true
---

# Issue reporting (mandatory)

- Follow `docs/AGENT_PROTOCOL.md` section 4 for what and how to report.
- Append to `docs/REPORT.md` only; never include secrets.

# Apply report

- Run `/apply-report` (defined in `.precursor/commands/apply-report.json`) to list unresolved/mitigated entries.
- Follow the remediation checklist in the command output.
