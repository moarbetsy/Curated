# machine-setup (compatibility wrapper)

This folder is kept for backwards compatibility.

The canonical source for setup-cursor now lives in:
- `packages/setup-cursor/`

If you previously ran scripts from `machine-setup/`, you can keep doing so — these wrappers forward to the canonical location.
